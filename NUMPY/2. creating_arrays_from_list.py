import numpy as np

from_list = np.array([1,2,3], dtype=np.int8) #I think that dtype is optional, just specifies how many bits you want bc theres 64bits in 1 int of this list which is obv too much
print(from_list)